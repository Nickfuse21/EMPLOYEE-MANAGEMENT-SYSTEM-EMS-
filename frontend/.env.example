# Base URL of the backend API.
# Leave as "/api" to use the Vite dev-server proxy (recommended for local dev).
# For a deployed frontend, set the full URL, e.g. https://your-api.com/api
VITE_API_BASE_URL=/api
